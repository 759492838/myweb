{
  "name": "ant-component",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "rolldown-vite",
    "build": "rolldown-vite build",
    "preview": "rolldown-vite preview",
    "prebuild": "rm -f yarn.lock && yarn install --frozen-lockfile",
    "deploy": "yarn prebuild && npx wrangler deploy"
  },
  "dependencies": {
    "ant-design-vue": "3.2.14",
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "npm:rolldown-vite@7.2.5"
  }
}
